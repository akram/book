name: generate-adr-index

on:
  workflow_dispatch:
  workflow_call:

jobs:
  generate-adr-index:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout docs repo
        uses: actions/checkout@v3

      - name: Generate ADR list
        run: for adr in *.md; do echo "* [$adr](./$adr)" > links.md; done
        working-directory: ./ADR

      # Basically, just to add a header.
      - name: Reformat ADR list
        run: echo "# Architecture Decision Records (ADRs)" > index.md && cat links.md >> index.md
        working-directory: ./ADR

      - name: Upload generated index
        uses: actions/upload-artifact@v3
        with:
          name: adr-index
          path: ADR/